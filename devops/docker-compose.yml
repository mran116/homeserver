services:

  # ===========================================================================
  # GITEA - Self-hosted Git + Actions CI
  # COMMENTED OUT - Phase 3
  # When enabled:
  #   - Host this repo locally for offline-capable GitOps
  #   - Run compose-lint / yamllint / gitleaks on every push
  #   - Mirror from GitHub if you want a public fork too
  # ===========================================================================
  # gitea:
  #   image: gitea/gitea:latest
  #   container_name: gitea
  #   restart: unless-stopped
  #   environment:
  #     - USER_UID=${PUID}
  #     - USER_GID=${PGID}
  #     - TZ=${TZ}
  #     - GITEA__database__DB_TYPE=postgres
  #     - GITEA__database__HOST=gitea-db:5432
  #     - GITEA__database__NAME=gitea
  #     - GITEA__database__USER=gitea
  #     - GITEA__database__PASSWD=${GITEA_DB_PASSWORD}
  #   volumes:
  #     - ${CONFIG_PATH}/gitea:/data
  #     - /etc/timezone:/etc/timezone:ro
  #     - /etc/localtime:/etc/localtime:ro
  #   ports:
  #     - "${GITEA_HTTP_PORT}:3000"
  #     - "${GITEA_SSH_PORT}:22"
  #   depends_on:
  #     - gitea-db
  #   networks:
  #     - home

  # gitea-db:
  #   image: postgres:16
  #   container_name: gitea-db
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_USER=gitea
  #     - POSTGRES_PASSWORD=${GITEA_DB_PASSWORD}
  #     - POSTGRES_DB=gitea
  #   volumes:
  #     - ${CONFIG_PATH}/gitea/db:/var/lib/postgresql/data
  #   networks:
  #     - home

  # gitea-runner:
  #   image: gitea/act_runner:latest
  #   container_name: gitea-runner
  #   restart: unless-stopped
  #   environment:
  #     - TZ=${TZ}
  #     - GITEA_INSTANCE_URL=http://gitea:3000
  #     - GITEA_RUNNER_REGISTRATION_TOKEN=${GITEA_RUNNER_TOKEN}
  #     - GITEA_RUNNER_NAME=homeserver-runner
  #     - CONFIG_FILE=/config/config.yaml
  #   volumes:
  #     - ${CONFIG_PATH}/gitea-runner:/data
  #     - /var/run/docker.sock:/var/run/docker.sock
  #   depends_on:
  #     - gitea
  #   networks:
  #     - home

networks:
  home:
    external: true
